// Central frontend configuration.
// For local development set window.API_BASE_URL in browser console or edit here.
const API = window.API_BASE_URL || "https://code-and-conquer-saas-11g6.onrender.com";
const TOKEN_EXPIRY = 60 * 60 * 1000; // 1 hour fallback
